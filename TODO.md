# TODO List

- Add more flexible compression.
Instead of archiving a whole directory,   
we should send each file in the directory seperetly.
This ensures we don't have to wait for the archiving and compression to complete    
before we can start sending the data.

- Update UI (Client & Server)
- Server destination option
- Encryption
- Support record types other than TXT
- Add support for bigger files
